#!/usr/bin/python
#encoding=utf-8

import sys
import os
import socket
import MySQLdb
from threading import Thread
import re
import socket

def main():
    con=MySQLdb.connect(host='localhost',user='root',passwd='19941017')
    cur=con.cursor()
    con.select_db('practice')
    i=396
    j=5
    sql="select * from Ttable where id='%s' and tid='%s'"%(i,j)
    cur.execute(sql)
    result=cur.fetchall()
    if result:
	print 1
                
        
if __name__=='__main__':
    main()
         
        
